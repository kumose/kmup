# KMup - Docker

Dockerfile is found in the root of the repository.

Docker image can be found on [docker hub](https://hub.docker.com/r/kmup/kmup).

Documentation on using docker image can be found on [Kmup Docs site](https://pub.kumose.cc/kmup/installation/install-with-docker-rootless).
